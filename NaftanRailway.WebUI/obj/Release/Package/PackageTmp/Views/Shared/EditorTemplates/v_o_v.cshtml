@*@*@using NaftanRailway.Domain.Concrete.DbContext.OBD
@*@model List<v_o_v>*@
@{
  @*  Layout = null;
}*@
<!--<style>
    .checkBoxList {
         max-height: 105px;
         height: auto !important;
         height: 105px;
     }
</style>

<label>@Html.DisplayNameFor(m => m)</label>

<div class="checkBoxList" style="OVERFLOW-Y: scroll; WIDTH: auto; HEIGHT: auto">
@//if (Model != null && Model.Count != 0) {  
//    for (int i = 0; i < Model.Count; i++) {
         <div class="input-group">
             <span class="input-group-addon">
                   @*{ @Html.CheckBox("[" + i + "]", true)
                    @Html.HiddenFor(m=>m[i].id)
                    @Html.HiddenFor(m=>m[i].id_otpr)
                    @Html.DisplayFor(m => m[i].n_vag)}*@
            </span>
            <span class="form-control">
@*                    @foreach (var prop in Model[i].GetType().GetProperties()) {
                        Html.ViewContext.ViewData.TemplateInfo.HtmlFieldPrefix = "DocumentPackLine.WagonsNumbers[" + i + "]";
                        @Html.Hidden(prop.Name, prop.GetValue(Model[i], null))
                    }*@
               @* @using (Html.BeginForm("RemoveWagons","Wagons")) {
                    @Html.Hidden("id",line.Shipping.id)
                    @Html.HiddenFor(m => m.ReturnUrl)*@
                    <input class="btn btn-sm btn-warning" type="submit" value="Удалить"/>
                }
            </span>
         </div>
    }
} else {
    @Html.HiddenFor(m => m)
}
<span>

</span>
</div>
    *@*@ -->