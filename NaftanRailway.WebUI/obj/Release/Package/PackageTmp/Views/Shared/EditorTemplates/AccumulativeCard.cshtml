@*@model List<NaftanRailway.Domain.BusinessModels.AccumulativeCard>

@{
    Layout = null;
}

<style>
    .checkBoxList {
        max-height: 105px;
        height: auto !important;
        height: 105px;
    }
</style>

<label>@Html.DisplayNameFor(m => m)</label>
<div class="checkBoxList" style="OVERFLOW-Y:scroll; WIDTH:auto; HEIGHT:auto">
    @foreach (var c in Model) {
        <div class="input-group">
            <span class="input-group-addon">
                @Html.CheckBox("", true)
            </span>
            <span class="form-control">
                @Html.DisplayFor(m => c.VKart.num_kart, new { })
            </span>
        </div>
    }
</div>*@

