@model NaftanRailway.Domain.Concrete.DbContexts.OBD.v_o_v
@{
    Layout = null;
    Html.ViewContext.ViewData.TemplateInfo.HtmlFieldPrefix = "DocumentPackLine.WagonsNumbers[" + ViewData["index"] + "]";
}
<div class="input-group">
    <span class="input-group-addon">
        @Html.CheckBoxFor(x => x.IsSelected,new {@checked = true })
    </span> 
    <span class="form-control">
        @Html.DisplayFor(m => m.n_vag)
        @Html.HiddenFor(w=>w.id)
        @Html.HiddenFor(w=>w.id_otpr)
        @Html.HiddenFor(w=>w.n_vag)
    </span>
</div>         